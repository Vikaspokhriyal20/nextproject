export const metadata = {
  title: 'About',
}

const page = () => {
  return (
    <section>
      <div className="container">
        <p>About Page</p>
      </div>
    </section>

  )
}

export default page
