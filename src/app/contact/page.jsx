export const metadata = {
    title: 'Contact',
}

const page = () => {
    return (
        <section>
            <div className="container">
                <p>Contact Page</p>
            </div>
        </section>

    )
}

export default page
